{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability set for the main manual viewer window",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
